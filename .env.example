# Example environment variables for backend-wizards-profile
# Server port
PORT=3000

# User info
EMAIL=nwosudavid77@gmail.com
FULL_NAME="Nwosu David"
STACK="Node.js/Express"

# Cat facts API configuration
CATFACT_URL=https://catfact.ninja/fact
CATFACT_TIMEOUT_MS=5000

# Fallback fact (if set, the server will return this when external API fails)
FACT_FALLBACK="No cat fact available at the moment."
